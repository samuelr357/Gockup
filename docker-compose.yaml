version: '3.8'

services:
  mysql-backup-system:
    build: .
    network_mode: "host"
    volumes:
      - ./mysql-backup-config:/root/.mysql-backup-config:ro
      - ./backups:/app/backups
      - ./logs:/app/logs
      - ./ssh:/root/.ssh:ro
    restart: unless-stopped
